[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "supreme-modeltx"
version = "0.1.0"
description = "Supreme Model TX training-oriented baseline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
